from enum import Enum

class FileAttribute(Enum):
    """
    An enumeration of common attributes associated with a file
    """
    NAME = "file.name"
    VERSION = "file.version"
    TYPE = "file.type"